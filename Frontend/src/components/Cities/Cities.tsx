import Navbar from "../Navbar/Navbar";

const Cities = () => {
  return (
    <div className="flex flex-col min-h-screen">
      <Navbar />
      <div>Cities</div>
    </div>
  );
};

export default Cities;
